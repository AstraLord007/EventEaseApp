@page "/about"
@using EventEaseApp.Data
@inject SessionService SessionService

<div class="about-header">
    <img src="favicon.png" alt="Logo" class="about-logo" />
    <h1 class="about-title">EventEase</h1>
</div>

<h3>Sobre nosotros</h3>

<p>
    <strong>EventEase</strong> es una aplicación diseñada para simplificar la gestión de eventos.
    Permite a los usuarios registrarse, crear eventos, confirmar o cancelar asistencia y editar detalles.
</p>

<p>
    El objetivo es ofrecer una experiencia clara y eficiente para organizar y participar en eventos,
    manteniendo siempre la sesión activa hasta que el usuario decida cerrar sesión.
</p>

@if (SessionService.IsLoggedIn)
{
    <p><em>Sesión activa como:</em> @SessionService.GetCurrentUser()?.Name</p>
}